{{ log_dir }}/{{ service_name }}-access.log {{ log_dir }}/{{ service_name }}-error.log {
    daily
    rotate 60
    missingok
    notifempty
    compress
    compresscmd /bin/bzip2
    copytruncate
    dateext
}
